torch
transformers
safetensors
pytest

# Optional: install ninetoothed for --backend ninetoothed, or TileLang for
# --backend tilelang. The bundled MACA C++ backend is built separately.
# ninetoothed
# tilelang
